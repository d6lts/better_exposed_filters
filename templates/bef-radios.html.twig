{#
  Themes a single-select exposed form element as radio buttons.

  Available variables:
    - element: the radio buttons to be rendered.
    - bef_nested: (bool) Whether this is a nested list as defined by '-'
                  character prefixes.
#}
{% if bef_nested %}
  Not yet implemented...
{% else %}
  {{ element }}
{% endif %}
